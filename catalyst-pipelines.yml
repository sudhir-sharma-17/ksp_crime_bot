version: 1.0
jobs:
  build_and_deploy:
    steps:
      - cd backend
      - npm install
      - npm run build --if-present
      - catalyst deploy --app aloka-backend
stages:
  - name: deploy_stage
    jobs:
      - build_and_deploy
version: 1.0
jobs:
  build_and_deploy:
    steps:
      - npm install -g zcatalyst-cli
      - cd backend
      - npm install
      - npm run build --if-present
      - catalyst deploy
stages:
  - name: deploy_stage
    jobs:
      - build_and_deploy
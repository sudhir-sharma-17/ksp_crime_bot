version: 1.0
stages:
  - name: Build and Deploy Chatbot
    actions:
      - name: Deploy Frontend and Backend
        type: catalyst-deploy
        inputs:
          # This instructs Zoho to read your root catalyst.json file
          # and deploy all packages (AppSail backend & React frontend)
          command: catalyst deploy --all
